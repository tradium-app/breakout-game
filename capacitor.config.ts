import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tradiumapp.breakout.qa",
  appName: "breakout-game",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
